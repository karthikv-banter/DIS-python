from pubsub import pub


# def publish_message(signal, sender, banterMessage):
#     return pub.sendMessage('test_topic',
#                     signal = signal, sender = sender, banterMessage = banterMessage)
